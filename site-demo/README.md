# OpenBusiness GUI Demo

Static GUI demo for the OpenBusiness website concept.

Open directly:

```bash
open site-demo/index.html
```

The demo uses existing SVG assets from `docs/assets/` and GSAP from a CDN for
pipeline, evidence-meter, tab, and dashboard animations.
